{
  "name": "claudecode.link",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc && vite build",
    "deploy": "npm run build && wrangler deploy",
    "db:init": "wrangler d1 create claude-sessions",
    "db:migrations": "wrangler d1 migrations apply DB --local"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250110.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "wrangler": "^4.41.0"
  }
}
